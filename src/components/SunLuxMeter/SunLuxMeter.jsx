import React from 'react';
import { useState } from 'react';
import { useDispatch, useSelector } from 'react-redux';
import { useHistory, useParams } from 'react-router-dom';
import { Table, TableBody, TableCell, TableContainer, Typography, TableHead, TableRow, Paper, Button, ButtonGroup } from '@mui/material';

function SunLuxMeter() {
//Intent of this feature is to have a separate page at first
//to use the ambient sensor to caputre lux data,
//then integrate this feature as a button on the form to capture
//beginning and ending exposure

    return (
<div className="container">
    
</div>
    );
}

export default SunLuxMeter;